/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.shields.io'], // Adiciona o domínio img.shields.io à lista de permitidos
    domains: ['lh3.googleusercontent.com'],
  },
};

export default nextConfig;
